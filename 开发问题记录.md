# GNSS-RO 大气剖面反演系统 - 开发问题记录

本文档记录项目开发过程中遇到的主要问题及解决方案。

---

## 问题 1: FY-3D GNOS L1 数据弯曲角推导失败

### 问题描述
- **时间**: 项目初期
- **现象**: 使用几何光学方法从 L1 级原始观测数据推导弯曲角时，得到负值（~-480 mrad），与预期正值（~5-20 mrad）严重不符
- **影响**: 无法使用 FY-3D GNOS L1 数据进行训练

### 问题原因
1. L1 数据为原始观测量，需要复杂的处理流程：
   - excess phase → 电离层校正 → Doppler → 几何光学反演
2. excess phase 的符号约定不明确
3. 开环追踪数据（50 Hz）可能需要特殊处理（Canonical Transform / Phase Matching）
4. 坐标系转换（ECI）和速度投影方向处理复杂

### 解决方案
**改用 FY-3D GNOS L2 ATP（大气廓线）数据**
- L2 ATP 是已处理的产品，直接包含：
  - 弯曲角 (Opt_Bend_ang)
  - 温度 (Temp)
  - 气压 (Pres)
  - 高度 (MSL_alt)
- 无需自行推导弯曲角，大大简化了数据处理流程

### 结果
- 成功处理 15,317 个 L2 ATP 文件
- 通过率 85.4%（13,088 个有效廓线）
- 数据质量良好，可直接用于训练

---

## 问题 2: ATP 数据处理中的 NaN 值问题

### 问题描述
- **时间**: ATP 数据处理阶段
- **现象**: 训练时所有 epoch 的损失都是 NaN，模型无法学习
- **数据检查**: 发现 train_x 中有 180,714 个 NaN，train_y 中有 210,000 个 NaN

### 问题原因
在 `interpolate_to_grid` 函数中，使用了 `np.interp` 的默认外推参数：
```python
interp_values = np.interp(
    target_heights,
    heights,
    values,
    left=np.nan,   # 低于数据范围时返回 NaN
    right=np.nan   # 高于数据范围时返回 NaN
)
```
当目标高度网格（0-60 km）超出实际数据范围时，会产生 NaN 值。

### 解决方案
**修改外推策略为边界值外推**：
```python
interp_values = np.interp(
    target_heights,
    heights,
    values,
    left=values[0],   # 使用最低高度的值
    right=values[-1]  # 使用最高高度的值
)
```

### 结果
- 完全消除了数据中的 NaN 值
- 训练可以正常进行

---

## 问题 3: 数据通过率从 85% 骤降至 8%

### 问题描述
- **时间**: 修复 NaN 问题后
- **现象**: 修改外推策略后，数据质量检查的通过率从 85.4% 骤降至 7.9%
- **影响**: 可用训练样本大幅减少

### 问题原因
严格的气压单调性检查与边界外推冲突：
```python
# 原始检查：要求气压完全单调递减
if not np.all(np.diff(pres_valid) < 0):
    return False
```
边界值外推可能导致气压在边界处出现小幅波动，无法通过严格的单调性检查。

### 解决方案
**放宽质量控制标准**：
```python
def _check_physical_validity(self, temp, pres, bend_ang):
    # 温度范围: 150-350 K
    if np.nanmin(temp) < 150 or np.nanmax(temp) > 350:
        return False

    # 气压范围: 0.01-1100 mb
    if np.nanmin(pres) < 0.01 or np.nanmax(pres) > 1100:
        return False

    # 放宽的气压单调性检查
    pres_valid = pres[~np.isnan(pres)]
    if len(pres_valid) > 10:
        pres_diff = np.diff(pres_valid)
        # 允许最多 20% 的点有小幅增加
        increasing_points = pres_diff > 0
        if increasing_points.sum() > len(pres_diff) * 0.2:
            return False
        # 检查大幅增加（> 10% 相对变化）
        if np.any(pres_diff > pres_valid[:-1] * 0.1):
            return False

    return True
```

### 结果
- 通过率恢复到 86.2%（13,207 个廓线）
- 训练集: 9,244 / 验证集: 1,981 / 测试集: 1,982
- 数据质量和数量都得到保证

---

## 问题 4: 高度坐标系不匹配

### 问题描述
- **时间**: ATP 数据处理阶段
- **现象**: 插值失败，高度范围不重叠
- **数据检查**: Impact_parm 值在 6347-6446 km 范围，而目标高度网格是 0-60 km

### 问题原因
ATP 数据中的 `Opt_Impact_parm` 是**地心距离**（geocentric distance），不是海拔高度（altitude）。

### 解决方案
**进行坐标转换**：
```python
# 读取曲率半径
curv = float(ds.getncattr('curv'))

# 转换为海拔高度
impact_parm = ds.variables['Opt_Impact_parm'][:]
msl_alt_bend = impact_parm - curv  # 地心距离 - 曲率半径 = 海拔
```

### 结果
- 高度范围正确对齐到 0-60 km
- 插值可以正常进行

---

## 问题 5: 模型推理结果质量极差

### 问题描述
- **时间**: 首次推理阶段
- **现象**:
  - 温度 RMSE: 66.21 K（误差巨大）
  - 气压 RMSE: 739.22 mb（误差极高）
  - 相关系数接近 0（预测几乎随机）
  - 出现物理上不可能的值：温度 -26 K ~ 516 K，气压为负值
- **矛盾**: 训练验证损失很低（0.015），但推理结果很差

### 问题诊断过程

#### 步骤 1: 检查预测值统计
```python
# 标准化空间的预测统计
预测温度: mean=0.0565, std=2.7230  # std 应该≈1
预测气压: mean=0.3490, std=3.1432  # std 应该≈1
真值温度: mean=0.0032, std=1.0062  # 正常
真值气压: mean=0.0011, std=1.0002  # 正常
```
**发现**: 预测值的标准差远大于 1，说明模型输出噪声过大。

#### 步骤 2: 测试单步噪声预测能力
创建测试脚本，在不同时间步测试模型预测噪声的准确性：

| 时间步 | MSE | 相关系数 | 性能评价 |
|--------|-----|----------|----------|
| t=0 (无噪声) | 0.198 | 0.896 | **差** |
| t=100 | 0.039 | 0.981 | 良好 |
| t=500 | 0.009 | 0.996 | 优秀 |
| t=999 (纯噪声) | 0.0002 | 0.9999 | **近乎完美** |

**关键发现**: 模型在高时间步（t=999）表现完美，但在低时间步（t=0）表现很差！

### 问题根因
**扩散模型的时间步性能不平衡**：

1. **训练时均匀采样所有时间步**，但任务难度不同：
   - t=999：预测纯噪声很容易
   - t=0：需要保留精细细节，很困难

2. **推理质量取决于低时间步性能**：
   - DDIM/DDPM 采样从 t=999（模型强项）开始
   - 逐步去噪到 t=0（模型弱项）
   - 最终质量由低时间步决定

3. **验证损失具有误导性**：
   - 验证损失 0.015 是所有时间步的平均
   - 掩盖了低时间步性能差的问题

### 尝试的解决方案

#### 方案 1: DDPM 完整采样（1000 步）
- **思路**: 使用完整的 1000 步采样替代 DDIM 的 50 步
- **结果**: 任务卡住，无法完成
- **状态**: ✗ 失败

#### 方案 2: 时间步加权训练
- **思路**: 训练时给低时间步更高权重
  ```python
  # 线性权重：t=0 权重 5.0，t=999 权重 1.0
  weight = 5.0 - 4.0 * (t / timesteps)
  ```
- **配置**:
  - 学习率降低到 5e-5
  - 训练 150 轮
  - patience=30
- **结果**: 训练被提前终止，测试显示低时间步性能未改善（-3.1%）
- **状态**: ✗ 失败（可能需要更长训练时间）

### 当前状态
问题尚未解决，需要进一步尝试：
- 增加训练数据量
- 延长训练时间（300+ 轮）
- 调整模型架构（增加容量）
- 或采用其他训练策略

---

## 经验总结

### 数据处理
1. **优先使用高级别数据产品**: L2 级数据比 L1 级更可靠，避免复杂的推导过程
2. **注意坐标系**: 地心距离 ≠ 海拔高度，需要转换
3. **外推策略要合理**: 边界值外推比 NaN 更安全
4. **质量控制要平衡**: 过严会损失数据，过松会引入噪声

### 模型训练
1. **验证损失不是唯一指标**: 需要针对性测试关键性能（如低时间步）
2. **扩散模型的时间步不平衡**: 需要特别关注低时间步性能
3. **训练策略需要充分时间**: 加权训练等策略需要足够的训练轮数才能生效

### 调试方法
1. **分步诊断**: 从数据统计 → 单步预测 → 完整推理，逐步定位问题
2. **可视化很重要**: 散点图、剖面图能直观发现问题
3. **对比测试**: 原始模型 vs 改进模型的对比能明确改进效果

---

*最后更新: 2026-03-03*
