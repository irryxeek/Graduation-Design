# GNSS-RO 大气剖面反演系统 - 开发问题记录

本文档记录项目开发过程中遇到的主要问题及解决方案。

---

## 问题 1: FY-3D GNOS L1 数据弯曲角推导失败

### 问题描述
- **时间**: 项目初期
- **现象**: 使用几何光学方法从 L1 级原始观测数据推导弯曲角时，得到负值（~-480 mrad），与预期正值（~5-20 mrad）严重不符
- **影响**: 无法使用 FY-3D GNOS L1 数据进行训练

### 问题原因
1. L1 数据为原始观测量，需要复杂的处理流程：
   - excess phase → 电离层校正 → Doppler → 几何光学反演
2. excess phase 的符号约定不明确
3. 开环追踪数据（50 Hz）可能需要特殊处理（Canonical Transform / Phase Matching）
4. 坐标系转换（ECI）和速度投影方向处理复杂

### 解决方案
**改用 FY-3D GNOS L2 ATP（大气廓线）数据**
- L2 ATP 是已处理的产品，直接包含：
  - 弯曲角 (Opt_Bend_ang)
  - 温度 (Temp)
  - 气压 (Pres)
  - 高度 (MSL_alt)
- 无需自行推导弯曲角，大大简化了数据处理流程

### 结果
- 成功处理 15,317 个 L2 ATP 文件
- 通过率 85.4%（13,088 个有效廓线）
- 数据质量良好，可直接用于训练

---

## 问题 2: ATP 数据处理中的 NaN 值问题

### 问题描述
- **时间**: ATP 数据处理阶段
- **现象**: 训练时所有 epoch 的损失都是 NaN，模型无法学习
- **数据检查**: 发现 train_x 中有 180,714 个 NaN，train_y 中有 210,000 个 NaN

### 问题原因
在 `interpolate_to_grid` 函数中，使用了 `np.interp` 的默认外推参数：
```python
interp_values = np.interp(
    target_heights,
    heights,
    values,
    left=np.nan,   # 低于数据范围时返回 NaN
    right=np.nan   # 高于数据范围时返回 NaN
)
```
当目标高度网格（0-60 km）超出实际数据范围时，会产生 NaN 值。

### 解决方案
**修改外推策略为边界值外推**：
```python
interp_values = np.interp(
    target_heights,
    heights,
    values,
    left=values[0],   # 使用最低高度的值
    right=values[-1]  # 使用最高高度的值
)
```

### 结果
- 完全消除了数据中的 NaN 值
- 训练可以正常进行

---

## 问题 3: 数据通过率从 85% 骤降至 8%

### 问题描述
- **时间**: 修复 NaN 问题后
- **现象**: 修改外推策略后，数据质量检查的通过率从 85.4% 骤降至 7.9%
- **影响**: 可用训练样本大幅减少

### 问题原因
严格的气压单调性检查与边界外推冲突：
```python
# 原始检查：要求气压完全单调递减
if not np.all(np.diff(pres_valid) < 0):
    return False
```
边界值外推可能导致气压在边界处出现小幅波动，无法通过严格的单调性检查。

### 解决方案
**放宽质量控制标准**：
```python
def _check_physical_validity(self, temp, pres, bend_ang):
    # 温度范围: 150-350 K
    if np.nanmin(temp) < 150 or np.nanmax(temp) > 350:
        return False

    # 气压范围: 0.01-1100 mb
    if np.nanmin(pres) < 0.01 or np.nanmax(pres) > 1100:
        return False

    # 放宽的气压单调性检查
    pres_valid = pres[~np.isnan(pres)]
    if len(pres_valid) > 10:
        pres_diff = np.diff(pres_valid)
        # 允许最多 20% 的点有小幅增加
        increasing_points = pres_diff > 0
        if increasing_points.sum() > len(pres_diff) * 0.2:
            return False
        # 检查大幅增加（> 10% 相对变化）
        if np.any(pres_diff > pres_valid[:-1] * 0.1):
            return False

    return True
```

### 结果
- 通过率恢复到 86.2%（13,207 个廓线）
- 训练集: 9,244 / 验证集: 1,981 / 测试集: 1,982
- 数据质量和数量都得到保证

---

## 问题 4: 高度坐标系不匹配

### 问题描述
- **时间**: ATP 数据处理阶段
- **现象**: 插值失败，高度范围不重叠
- **数据检查**: Impact_parm 值在 6347-6446 km 范围，而目标高度网格是 0-60 km

### 问题原因
ATP 数据中的 `Opt_Impact_parm` 是**地心距离**（geocentric distance），不是海拔高度（altitude）。

### 解决方案
**进行坐标转换**：
```python
# 读取曲率半径
curv = float(ds.getncattr('curv'))

# 转换为海拔高度
impact_parm = ds.variables['Opt_Impact_parm'][:]
msl_alt_bend = impact_parm - curv  # 地心距离 - 曲率半径 = 海拔
```

### 结果
- 高度范围正确对齐到 0-60 km
- 插值可以正常进行

---

## 问题 5: 模型推理结果质量极差

### 问题描述
- **时间**: 首次推理阶段
- **现象**:
  - 温度 RMSE: 66.21 K（误差巨大）
  - 气压 RMSE: 739.22 mb（误差极高）
  - 相关系数接近 0（预测几乎随机）
  - 出现物理上不可能的值：温度 -26 K ~ 516 K，气压为负值
- **矛盾**: 训练验证损失很低（0.015），但推理结果很差

### 问题诊断过程

#### 步骤 1: 检查预测值统计
```python
# 标准化空间的预测统计
预测温度: mean=0.0565, std=2.7230  # std 应该≈1
预测气压: mean=0.3490, std=3.1432  # std 应该≈1
真值温度: mean=0.0032, std=1.0062  # 正常
真值气压: mean=0.0011, std=1.0002  # 正常
```
**发现**: 预测值的标准差远大于 1，说明模型输出噪声过大。

#### 步骤 2: 测试单步噪声预测能力
创建测试脚本，在不同时间步测试模型预测噪声的准确性：

| 时间步 | MSE | 相关系数 | 性能评价 |
|--------|-----|----------|----------|
| t=0 (无噪声) | 0.198 | 0.896 | **差** |
| t=100 | 0.039 | 0.981 | 良好 |
| t=500 | 0.009 | 0.996 | 优秀 |
| t=999 (纯噪声) | 0.0002 | 0.9999 | **近乎完美** |

**关键发现**: 模型在高时间步（t=999）表现完美，但在低时间步（t=0）表现很差！

### 问题根因
**扩散模型的时间步性能不平衡**：

1. **训练时均匀采样所有时间步**，但任务难度不同：
   - t=999：预测纯噪声很容易
   - t=0：需要保留精细细节，很困难

2. **推理质量取决于低时间步性能**：
   - DDIM/DDPM 采样从 t=999（模型强项）开始
   - 逐步去噪到 t=0（模型弱项）
   - 最终质量由低时间步决定

3. **验证损失具有误导性**：
   - 验证损失 0.015 是所有时间步的平均
   - 掩盖了低时间步性能差的问题

### 尝试的解决方案

#### 方案 1: DDPM 完整采样（1000 步）
- **思路**: 使用完整的 1000 步采样替代 DDIM 的 50 步
- **结果**: 任务卡住，无法完成
- **状态**: ✗ 失败

#### 方案 2: 时间步加权训练
- **思路**: 训练时给低时间步更高权重
  ```python
  # 线性权重：t=0 权重 5.0，t=999 权重 1.0
  weight = 5.0 - 4.0 * (t / timesteps)
  ```
- **配置**:
  - 学习率降低到 5e-5
  - 训练 150 轮
  - patience=30
- **结果**: 训练被提前终止，测试显示低时间步性能未改善（-3.1%）
- **状态**: ✗ 失败（可能需要更长训练时间）

### 当前状态
问题尚未解决，需要进一步尝试：
- 增加训练数据量
- 延长训练时间（300+ 轮）
- 调整模型架构（增加容量）
- 或采用其他训练策略

---

## 经验总结

### 数据处理
1. **优先使用高级别数据产品**: L2 级数据比 L1 级更可靠，避免复杂的推导过程
2. **注意坐标系**: 地心距离 ≠ 海拔高度，需要转换
3. **外推策略要合理**: 边界值外推比 NaN 更安全
4. **质量控制要平衡**: 过严会损失数据，过松会引入噪声

### 模型训练
1. **验证损失不是唯一指标**: 需要针对性测试关键性能（如低时间步）
2. **扩散模型的时间步不平衡**: 需要特别关注低时间步性能
3. **训练策略需要充分时间**: 加权训练等策略需要足够的训练轮数才能生效

### 调试方法
1. **分步诊断**: 从数据统计 → 单步预测 → 完整推理，逐步定位问题
2. **可视化很重要**: 散点图、剖面图能直观发现问题
3. **对比测试**: 原始模型 vs 改进模型的对比能明确改进效果

---

## 问题 6: 气压数据归一化不当导致预测失败

### 问题描述
- **时间**: 2026-03-04 最新训练
- **现象**:
  - 气压预测结果完全错误，相关系数接近 0
  - 预测值范围异常，与真实值分布严重不符
  - 温度预测也受影响

### 问题原因
**气压数据跨越 4 个数量级**（0.07 ~ 1099.76 mb），直接使用 Z-Score 标准化导致：
- 低层气压（>100 mb）主导均值和标准差
- 高层气压（<1 mb）被压缩到极小范围
- 标准化后分布严重不对称：[-0.55, 4.00]

```python
# 原始方法（错误）
pressure_normalized = (pressure - mean) / std
# 结果：低层气压占主导，高层气压信息丢失
```

### 解决方案
**采用 log10 变换 + Z-Score 标准化**：

```python
# 修正方法
pressure_log = np.log10(pressure)
pressure_normalized = (pressure_log - mean_log) / std_log
# 反归一化: pressure = 10^(pressure_normalized * std_log + mean_log)
```

**效果对比**:

| 方法 | 标准化后范围 | 分布对称性 | 气压 RMSE | 气压相关系数 |
|------|-------------|-----------|----------|------------|
| 直接 Z-Score | [-0.55, 4.00] | 严重右偏 | >300 mb | ~0.0 |
| log10 + Z-Score | [-2.09, 1.79] | 对称 | 146.82 mb | **0.75** |

### 结果
- 气压预测相关系数从 ~0.0 提升到 **0.75**
- 气压 RMSE 降低到 146.82 mb
- 温度预测也有改善（相关系数 0.23）

### 经验教训
- **跨数量级数据必须先取对数**: 气压、密度等指数分布的物理量
- **检查标准化后的分布**: 应接近标准正态分布 N(0,1)
- **反归一化时注意逆变换**: 需要先反标准化再取指数

---

## 问题 7: DDIM 采样完全失败

### 问题描述
- **时间**: 2026-03-04 推理阶段
- **现象**:
  - DDIM 50 步加速采样产生完全错误的结果
  - 温度相关系数 ~0.0，气压相关系数 ~0.0
  - 预测值呈现随机噪声特征，无物理意义

**对比实验**:

| 采样方法 | 步数 | 温度 CC | 气压 CC | 推理时间 |
|---------|------|---------|---------|---------|
| DDPM | 1000 | 0.23 | 0.75 | ~30s/样本 |
| DDIM | 50 | ~0.0 | ~0.0 | ~2s/样本 |

### 问题原因
1. **训练-推理不匹配**: 模型在 DDPM 1000 步下训练，DDIM 加速采样改变了去噪轨迹
2. **η 参数设置**: DDIM 的随机性参数 η=0.0（确定性采样）可能不适合本任务
3. **步长选择**: 50 步可能不足以充分去噪

### 临时解决方案
**使用 DDPM 完整采样**:
- 保证推理质量，牺牲速度
- 1000 步采样耗时约 30 秒/样本
- 适用于离线批量处理

### 调试结果（2026-03-05）

**测试配置**: 50 个测试样本，Tesla V100 GPU

| 配置 | 温度 CC | 气压 CC | 温度 RMSE | 气压 RMSE | 推理速度 |
|------|---------|---------|-----------|-----------|----------|
| DDPM-1000 | **0.2421** | **0.7526** | 24.14 K | 143.98 mb | ~6.4s/batch |
| DDIM-50-eta0.0 | -0.0119 | -0.0447 | 50.64 K | 7034.10 mb | ~0.3s/batch |
| DDIM-100-eta0.0 | -0.0174 | -0.0513 | 49.60 K | 4994.83 mb | ~0.6s/batch |
| DDIM-200-eta0.0 | -0.0123 | -0.0518 | 48.98 K | 4212.04 mb | ~1.3s/batch |
| DDIM-100-eta0.5 | NaN | NaN | NaN | NaN | ~0.7s/batch |
| DDIM-200-eta0.5 | NaN | NaN | NaN | NaN | ~1.3s/batch |

**关键发现**:
1. **DDIM 完全失败**: 即使增加到 200 步，相关系数仍然为负值
2. **步数增加无效**: 从 50 步到 200 步，气压 RMSE 从 7034 降到 4212 mb，但仍远高于 DDPM 的 144 mb
3. **引入随机性导致崩溃**: η=0.5 时产生 NaN 值，采样过程数值不稳定
4. **DDPM 是唯一可用方案**: 必须使用完整 1000 步采样

### 根本原因分析

DDIM 失败的根本原因是**训练-推理不匹配**：
- 模型在 DDPM 的噪声调度下训练（每步都添加随机噪声）
- DDIM 使用确定性采样轨迹，跳过大部分时间步
- 模型在 DDIM 跳过的时间步上没有见过数据，预测不准确

### 解决方案

**短期**: 继续使用 DDPM 1000 步采样
- 优点: 结果可靠（气压 CC=0.75）
- 缺点: 速度慢（~30s/样本）

**长期**: 重新训练支持 DDIM 的模型
1. 使用 v-prediction 目标函数
2. 训练时模拟 DDIM 采样过程
3. 或使用 Consistency Models / Latent Diffusion 等更快的架构

---

## 问题 8: 温度预测精度低

### 问题描述
- **时间**: 2026-03-04 推理评估
- **现象**:
  - 温度相关系数仅 0.23（气压为 0.75）
  - 温度 RMSE 25.79 K（约 10% 相对误差）
  - 高层温度（>30 km）预测偏差大

### 可能原因
1. **物理耦合弱**: 弯曲角对温度的敏感度低于气压
   - 弯曲角 ∝ ∫ N(h) dh，其中 N ∝ P/T
   - 气压的垂直梯度更显著，更容易从弯曲角反演

2. **数据质量**: FY-3D ATP 温度数据可能存在系统性偏差

3. **模型容量**: 单一 U-Net 难以同时学习温度和气压的不同特征
   - 温度需要更多高频细节
   - 气压需要更多低频趋势

### 待优化方向
1. **多任务学习**: 为温度和气压设计独立的解码器分支
2. **损失函数加权**: 增加温度损失的权重
3. **增加训练数据**: 处理更多月份的 FY-3D ATP 数据（2-3 月）
4. **引入先验知识**: 添加物理约束（静力平衡方程等）

---

## 当前最佳实践总结

### 数据预处理（关键！）

```python
# 弯曲角
ba_log = np.log10(np.abs(bending_angle) + 1e-6)
ba_norm = (ba_log - mean_ba) / std_ba

# 气压（必须 log10 变换！）
pres_log = np.log10(pressure)
pres_norm = (pres_log - mean_pres) / std_pres

# 温度
temp_norm = (temperature - mean_temp) / std_temp
```

### 训练配置

- 模型: EnhancedConditionalUNet1D
- 数据集: FY-3D ATP 2025-01（13,207 样本）
- Batch Size: 64
- Learning Rate: 1e-4
- Early Stopping: patience=20
- 最佳验证损失: 0.016882 (Epoch 62)

### 推理配置

- **采样方法**: DDPM 1000 步（DDIM 暂不可用）
- **设备**: GPU（Tesla V100）
- **速度**: ~30 秒/样本

### 性能指标

| 变量 | RMSE | Bias | 相关系数 | 状态 |
|------|------|------|---------|------|
| 温度 | 25.79 K | -2.04 K | 0.23 | ⚠️ 待优化 |
| 气压 | 146.82 mb | -39.64 mb | 0.75 | ✅ 可接受 |

---

## 下一步计划

### 短期优化
1. ✅ 完成气压归一化修复
2. ✅ 完成 DDPM 推理验证
3. ✅ 调试 DDIM 采样（100/200 步）— **结论: DDIM 不可用，需重新训练**
4. ⬜ 改进温度预测（多任务学习/损失加权）

### 中期目标
1. ⬜ 扩展数据集（处理 2-3 月 FY-3D ATP 数据）
2. ⬜ 模型架构优化
3. ⬜ 与 CDAAC 产品对比评估
4. ⬜ 重新训练支持 DDIM 的模型（v-prediction）

---

## 问题 9: Q1 数据处理中气压 log10 变换缺失

### 问题描述
- **时间**: 2026-03-05 推理阶段
- **现象**:
  - 推理完成后气压结果全部为 NaN
  - 温度预测正常（CC=0.25），但气压预测失败
  - 统计信息显示气压均值 115.86，标准差 212.23（原始气压值范围）

### 问题原因
**数据处理不一致**：
- `ATPProcessor.process_single_profile()` 中气压数据**未进行 log10 变换**
- 但 `process_atp_all_months.py` 中标记了 `pressure_log_transformed: True`
- 推理时错误地对原始气压值（已标准化）进行了 `10^x` 运算，导致数值溢出产生 NaN

```python
# 错误的处理流程
Y = np.stack([temp_interp, pres_interp], axis=0)  # 气压未 log10
# 统计: mean=115.86, std=212.23 (原始气压)
# 标准化: (pres - 115.86) / 212.23
# 推理时反标准化: 10^((pred * 212.23) + 115.86) → 溢出 → NaN
```

### 解决方案
**修改 `ATPProcessor` 对气压进行 log10 变换**：

```python
# 修正后的代码
bend_ang_log = np.log10(np.abs(bend_ang_interp) + 1e-6)
pres_log = np.log10(pres_interp)  # 添加 log10 变换

X = bend_ang_log
Y = np.stack([temp_interp, pres_log], axis=0)  # 温度 + log10(气压)
```

### 当前状态
- ✅ 已修复 `ATPProcessor` 代码
- 🔄 正在重新处理 Q1 数据（1-3月，37,171 样本）
- ⬜ 待重新训练模型
- ⬜ 待重新推理评估

### 经验教训
- **数据预处理必须保持一致性**: 处理、训练、推理三个阶段的变换必须完全一致
- **及时验证统计信息**: 如果看到气压统计值在 100+ 范围，说明未进行 log10 变换
- **添加数值检查**: 在推理后应检查是否有 NaN 值，及时发现问题

---

## 当前工作进度（2026-03-05）

### 已完成
1. ✅ DDIM 采样调试（结论：不可用）
2. ✅ 数据扩展（1-3月 ATP 数据）
3. ✅ 模型训练（损失加权，验证损失 0.014026）
4. ✅ 发现并修复气压 log10 变换缺失问题

### 进行中
5. 🔄 重新处理 Q1 数据（修复后）

### 待完成
6. ⬜ 重新训练模型
7. ⬜ 重新推理评估
8. ⬜ 生成详细评估报告

### 预期最终结果
基于修复后的数据和损失加权：
- 温度相关系数：0.25-0.35
- 气压相关系数：0.75-0.80
- 整体 RMSE 显著降低

---

*最后更新: 2026-03-05 (用户外出前)*
